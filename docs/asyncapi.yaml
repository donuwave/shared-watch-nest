asyncapi: '2.6.0'
info:
  title: Shared Watch Realtime API
  version: '1.0.0'
  description: Socket.IO события для realtime-доменов Shared Watch.
defaultContentType: application/json
tags:
  - name: Room Presence
    description: Подключение к комнате, heartbeat и события online/offline.
  - name: Video Sync
    description: Источник видео, play/pause/seek и восстановление состояния видео.
  - name: Chat
    description: События текстового чата.
  - name: Voice
    description: События голосовой связи и WebRTC-сигналинга.
  - name: Whiteboard
    description: Режим рисования поверх стоп-кадра видео.
servers:
  local:
    url: http://localhost:9000/rooms
    protocol: socket.io
    description: Локальный Socket.IO namespace для realtime событий комнаты.
    security:
      - accessToken: []
channels:
  room:join:
    description: Клиент подключается к realtime-каналу комнаты после Socket.IO соединения.
    publish:
      operationId: joinRoom
      summary: Подключиться к presence-каналу комнаты.
      tags:
        - name: Room Presence
      message:
        $ref: '#/components/messages/RoomJoin'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkRoomAck'
  room:heartbeat:
    description: Heartbeat клиента для активного presence в комнате.
    publish:
      operationId: heartbeatRoom
      summary: Отметить текущее соединение как активное.
      tags:
        - name: Room Presence
      message:
        $ref: '#/components/messages/RoomHeartbeat'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkAck'
  presence:joined:
    description: Сервер отправляет событие участникам комнаты, когда пользователь подключился к presence.
    subscribe:
      operationId: presenceJoined
      summary: Получить событие подключения участника.
      tags:
        - name: Room Presence
      message:
        $ref: '#/components/messages/PresenceJoined'
    x-socket-namespace: /rooms
  presence:left:
    description: Сервер отправляет событие участникам комнаты, когда пользователь отключился от presence.
    subscribe:
      operationId: presenceLeft
      summary: Получить событие отключения участника.
      tags:
        - name: Room Presence
      message:
        $ref: '#/components/messages/PresenceLeft'
    x-socket-namespace: /rooms
  video:source-set:
    description: Клиент задает источник видео в комнате. Нужна роль owner или moderator.
    publish:
      operationId: setVideoSource
      summary: Задать источник видео.
      tags:
        - name: Video Sync
      message:
        $ref: '#/components/messages/VideoSourceSet'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/VideoStateAck'
  video:play:
    description: Клиент запускает воспроизведение. Нужна роль owner или moderator.
    publish:
      operationId: playVideo
      summary: Запустить воспроизведение.
      tags:
        - name: Video Sync
      message:
        $ref: '#/components/messages/VideoControl'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/VideoStateAck'
  video:pause:
    description: Клиент ставит воспроизведение на паузу. Нужна роль owner или moderator.
    publish:
      operationId: pauseVideo
      summary: Поставить воспроизведение на паузу.
      tags:
        - name: Video Sync
      message:
        $ref: '#/components/messages/VideoControl'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/VideoStateAck'
  video:seek:
    description: Клиент перематывает видео. Нужна роль owner или moderator.
    publish:
      operationId: seekVideo
      summary: Перемотать видео.
      tags:
        - name: Video Sync
      message:
        $ref: '#/components/messages/VideoControl'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/VideoStateAck'
  video:sync-request:
    description: Клиент запрашивает текущее состояние видео. Нужен активный участник комнаты.
    publish:
      operationId: requestVideoSync
      summary: Запросить текущее состояние видео.
      tags:
        - name: Video Sync
      message:
        $ref: '#/components/messages/VideoSyncRequest'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/VideoStateAck'
  video:source-changed:
    description: Сервер отправляет событие при изменении источника видео.
    subscribe:
      operationId: videoSourceChanged
      summary: Получить событие смены источника.
      tags:
        - name: Video Sync
      message:
        $ref: '#/components/messages/VideoState'
    x-socket-namespace: /rooms
  video:state:
    description: Сервер отправляет событие при изменении состояния воспроизведения.
    subscribe:
      operationId: videoStateChanged
      summary: Получить обновление состояния видео.
      tags:
        - name: Video Sync
      message:
        $ref: '#/components/messages/VideoState'
    x-socket-namespace: /rooms
  video:sync-state:
    description: Сервер отправляет событие конкретному клиенту после video:sync-request.
    subscribe:
      operationId: videoSyncState
      summary: Получить текущее состояние видео.
      tags:
        - name: Video Sync
      message:
        $ref: '#/components/messages/VideoStateOrNull'
    x-socket-namespace: /rooms
  chat:send:
    description: Клиент отправляет сообщение в чат комнаты.
    publish:
      operationId: sendChatMessage
      summary: Отправить сообщение.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatSend'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/ChatMessageAck'
  chat:edit:
    description: Клиент редактирует свое сообщение.
    publish:
      operationId: editChatMessage
      summary: Отредактировать сообщение.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatEdit'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/ChatMessageAck'
  chat:delete:
    description: Клиент удаляет сообщение. Разрешено автору, owner или moderator.
    publish:
      operationId: deleteChatMessage
      summary: Удалить сообщение.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatDelete'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/ChatMessageAck'
  chat:typing:
    description: Клиент отправляет typing indicator, сервер пересылает его остальным участникам.
    publish:
      operationId: sendChatTyping
      summary: Отправить typing indicator.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatTyping'
    subscribe:
      operationId: receiveChatTyping
      summary: Получить typing indicator.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatTypingEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkAck'
  chat:message:
    description: Сервер отправляет новое сообщение участникам комнаты.
    subscribe:
      operationId: receiveChatMessage
      summary: Получить новое сообщение.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatMessage'
    x-socket-namespace: /rooms
  chat:message-edited:
    description: Сервер отправляет обновленное сообщение после редактирования.
    subscribe:
      operationId: receiveEditedChatMessage
      summary: Получить отредактированное сообщение.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatMessage'
    x-socket-namespace: /rooms
  chat:message-deleted:
    description: Сервер отправляет tombstone сообщения после удаления.
    subscribe:
      operationId: receiveDeletedChatMessage
      summary: Получить удаленное сообщение.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatMessage'
    x-socket-namespace: /rooms
  chat:read:
    description: Клиент отмечает сообщения прочитанными, сервер рассылает read-state участникам комнаты.
    publish:
      operationId: markChatRead
      summary: Отметить сообщения прочитанными.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatRead'
    subscribe:
      operationId: receiveChatRead
      summary: Получить обновление read-state.
      tags:
        - name: Chat
      message:
        $ref: '#/components/messages/ChatReadState'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/ChatReadStateAck'
  voice:join:
    description: Клиент подключается к голосовой связи комнаты.
    publish:
      operationId: joinVoice
      summary: Подключиться к голосовой связи.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceRoom'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkRoomAck'
  voice:leave:
    description: Клиент выходит из голосовой связи комнаты.
    publish:
      operationId: leaveVoice
      summary: Выйти из голосовой связи.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceRoom'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkRoomAck'
  voice:joined:
    description: Сервер сообщает остальным участникам, что пользователь подключился к голосу.
    subscribe:
      operationId: voiceJoined
      summary: Получить событие подключения к голосу.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceUserEvent'
    x-socket-namespace: /rooms
  voice:left:
    description: Сервер сообщает остальным участникам, что пользователь вышел из голоса.
    subscribe:
      operationId: voiceLeft
      summary: Получить событие выхода из голоса.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceUserEvent'
    x-socket-namespace: /rooms
  voice:offer:
    description: Клиент отправляет WebRTC offer, сервер пересылает его targetUserId.
    publish:
      operationId: sendVoiceOffer
      summary: Отправить WebRTC offer.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceSignal'
    subscribe:
      operationId: receiveVoiceOffer
      summary: Получить WebRTC offer.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceSignalEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkAck'
  voice:answer:
    description: Клиент отправляет WebRTC answer, сервер пересылает его targetUserId.
    publish:
      operationId: sendVoiceAnswer
      summary: Отправить WebRTC answer.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceSignal'
    subscribe:
      operationId: receiveVoiceAnswer
      summary: Получить WebRTC answer.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceSignalEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkAck'
  voice:ice-candidate:
    description: Клиент отправляет ICE candidate, сервер пересылает его targetUserId.
    publish:
      operationId: sendVoiceIceCandidate
      summary: Отправить ICE candidate.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceIceCandidate'
    subscribe:
      operationId: receiveVoiceIceCandidate
      summary: Получить ICE candidate.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceIceCandidateEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkAck'
  voice:mute:
    description: Клиент сообщает self mute/unmute, сервер рассылает статус участникам комнаты.
    publish:
      operationId: sendVoiceMute
      summary: Отправить self mute/unmute.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceMute'
    subscribe:
      operationId: receiveVoiceMute
      summary: Получить mute status.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceMuteEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkAck'
  voice:mute-participant:
    description: Owner комнаты мутит или размучивает участника. Backend отправляет событие, фронт применяет mute.
    publish:
      operationId: muteVoiceParticipant
      summary: Замутить или размутить участника.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceMuteParticipant'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkAck'
  voice:participant-muted:
    description: Сервер сообщает участникам комнаты, что owner изменил mute-state участника.
    subscribe:
      operationId: voiceParticipantMuted
      summary: Получить mute-state участника.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceParticipantMuted'
    x-socket-namespace: /rooms
  voice:speaking:
    description: Клиент сообщает, говорит ли пользователь сейчас. Сервер пересылает это другим участникам комнаты.
    publish:
      operationId: sendVoiceSpeaking
      summary: Отправить speaking indicator.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceSpeaking'
    subscribe:
      operationId: receiveVoiceSpeaking
      summary: Получить speaking indicator.
      tags:
        - name: Voice
      message:
        $ref: '#/components/messages/VoiceSpeakingEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/OkAck'
  whiteboard:enable:
    description: Клиент включает whiteboard. Нужна room role owner или moderator, видео должно быть на паузе.
    publish:
      operationId: enableWhiteboard
      summary: Включить whiteboard.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardRoom'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/WhiteboardStateAck'
  whiteboard:disable:
    description: Клиент выключает whiteboard. Нужна room role owner или moderator.
    publish:
      operationId: disableWhiteboard
      summary: Выключить whiteboard.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardRoom'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/WhiteboardStateAck'
  whiteboard:clear:
    description: Клиент очищает whiteboard snapshot. Нужна room role owner или moderator.
    publish:
      operationId: clearWhiteboard
      summary: Очистить whiteboard.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardRoom'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/WhiteboardStateAck'
  whiteboard:state:
    description: Сервер отправляет участникам комнаты состояние whiteboard после enable, disable или clear.
    subscribe:
      operationId: whiteboardStateChanged
      summary: Получить состояние whiteboard.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardState'
    x-socket-namespace: /rooms
  whiteboard:stroke-start:
    description: Клиент начинает stroke, сервер пересылает событие остальным участникам комнаты.
    publish:
      operationId: startWhiteboardStroke
      summary: Начать whiteboard stroke.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardStrokeStart'
    subscribe:
      operationId: receiveWhiteboardStrokeStart
      summary: Получить начало whiteboard stroke.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardStrokeStartEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/WhiteboardStrokeStartAck'
  whiteboard:stroke-append:
    description: Клиент отправляет новые точки stroke, сервер пересылает событие остальным участникам комнаты.
    publish:
      operationId: appendWhiteboardStroke
      summary: Передать точки whiteboard stroke.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardStrokeAppend'
    subscribe:
      operationId: receiveWhiteboardStrokeAppend
      summary: Получить точки whiteboard stroke.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardStrokeAppendEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/WhiteboardStrokeAppendAck'
  whiteboard:stroke-end:
    description: Клиент завершает stroke, backend сохраняет его в snapshot и рассылает финальный stroke участникам комнаты.
    publish:
      operationId: endWhiteboardStroke
      summary: Завершить whiteboard stroke.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardStrokeEnd'
    subscribe:
      operationId: receiveWhiteboardStrokeEnd
      summary: Получить завершенный whiteboard stroke.
      tags:
        - name: Whiteboard
      message:
        $ref: '#/components/messages/WhiteboardStrokeEndEvent'
    x-socket-namespace: /rooms
    x-ack:
      $ref: '#/components/schemas/WhiteboardStrokeEndAck'
components:
  securitySchemes:
    accessToken:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Передай access token через Socket.IO auth.token или Authorization header.
  messages:
    RoomJoin:
      name: room:join
      payload:
        $ref: '#/components/schemas/RoomJoinPayload'
    RoomHeartbeat:
      name: room:heartbeat
      payload:
        type: object
        additionalProperties: false
    PresenceJoined:
      name: presence:joined
      payload:
        $ref: '#/components/schemas/RoomPresenceEvent'
    PresenceLeft:
      name: presence:left
      payload:
        $ref: '#/components/schemas/RoomPresenceEvent'
    VideoSourceSet:
      name: video:source-set
      payload:
        $ref: '#/components/schemas/VideoSourceSetPayload'
    VideoControl:
      name: video:control
      payload:
        $ref: '#/components/schemas/VideoControlPayload'
    VideoSyncRequest:
      name: video:sync-request
      payload:
        $ref: '#/components/schemas/VideoSyncRequestPayload'
    VideoState:
      name: video:state
      payload:
        $ref: '#/components/schemas/VideoState'
    VideoStateOrNull:
      name: video:sync-state
      payload:
        oneOf:
          - $ref: '#/components/schemas/VideoState'
          - type: 'null'
    ChatSend:
      name: chat:send
      payload:
        $ref: '#/components/schemas/ChatSendPayload'
    ChatEdit:
      name: chat:edit
      payload:
        $ref: '#/components/schemas/ChatEditPayload'
    ChatDelete:
      name: chat:delete
      payload:
        $ref: '#/components/schemas/ChatDeletePayload'
    ChatTyping:
      name: chat:typing
      payload:
        $ref: '#/components/schemas/ChatTypingPayload'
    ChatTypingEvent:
      name: chat:typing
      payload:
        $ref: '#/components/schemas/ChatTypingEvent'
    ChatMessage:
      name: chat:message
      payload:
        $ref: '#/components/schemas/ChatMessage'
    ChatRead:
      name: chat:read
      payload:
        $ref: '#/components/schemas/ChatReadPayload'
    ChatReadState:
      name: chat:read
      payload:
        $ref: '#/components/schemas/ChatReadState'
    VoiceRoom:
      name: voice:room
      payload:
        $ref: '#/components/schemas/VoiceRoomPayload'
    VoiceUserEvent:
      name: voice:user-event
      payload:
        $ref: '#/components/schemas/VoiceUserEvent'
    VoiceSignal:
      name: voice:signal
      payload:
        $ref: '#/components/schemas/VoiceSignalPayload'
    VoiceSignalEvent:
      name: voice:signal-event
      payload:
        $ref: '#/components/schemas/VoiceSignalEvent'
    VoiceIceCandidate:
      name: voice:ice-candidate
      payload:
        $ref: '#/components/schemas/VoiceIceCandidatePayload'
    VoiceIceCandidateEvent:
      name: voice:ice-candidate-event
      payload:
        $ref: '#/components/schemas/VoiceIceCandidateEvent'
    VoiceMute:
      name: voice:mute
      payload:
        $ref: '#/components/schemas/VoiceMutePayload'
    VoiceMuteEvent:
      name: voice:mute-event
      payload:
        $ref: '#/components/schemas/VoiceMuteEvent'
    VoiceMuteParticipant:
      name: voice:mute-participant
      payload:
        $ref: '#/components/schemas/VoiceMuteParticipantPayload'
    VoiceParticipantMuted:
      name: voice:participant-muted
      payload:
        $ref: '#/components/schemas/VoiceParticipantMutedEvent'
    VoiceSpeaking:
      name: voice:speaking
      payload:
        $ref: '#/components/schemas/VoiceSpeakingPayload'
    VoiceSpeakingEvent:
      name: voice:speaking
      payload:
        $ref: '#/components/schemas/VoiceSpeakingEvent'
    WhiteboardRoom:
      name: whiteboard:room
      payload:
        $ref: '#/components/schemas/WhiteboardRoomPayload'
    WhiteboardState:
      name: whiteboard:state
      payload:
        $ref: '#/components/schemas/WhiteboardState'
    WhiteboardStrokeStart:
      name: whiteboard:stroke-start
      payload:
        $ref: '#/components/schemas/WhiteboardStrokeStartPayload'
    WhiteboardStrokeStartEvent:
      name: whiteboard:stroke-start
      payload:
        $ref: '#/components/schemas/WhiteboardStrokeStartEvent'
    WhiteboardStrokeAppend:
      name: whiteboard:stroke-append
      payload:
        $ref: '#/components/schemas/WhiteboardStrokeAppendPayload'
    WhiteboardStrokeAppendEvent:
      name: whiteboard:stroke-append
      payload:
        $ref: '#/components/schemas/WhiteboardStrokeAppendEvent'
    WhiteboardStrokeEnd:
      name: whiteboard:stroke-end
      payload:
        $ref: '#/components/schemas/WhiteboardStrokeEndPayload'
    WhiteboardStrokeEndEvent:
      name: whiteboard:stroke-end
      payload:
        $ref: '#/components/schemas/WhiteboardStrokeEndEvent'
  schemas:
    Uuid:
      type: string
      format: uuid
      example: 8f6b0a84-82b7-4f39-a3d1-6dfb2c6e4421
    RoomJoinPayload:
      type: object
      required:
        - roomId
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
    VideoSyncRequestPayload:
      type: object
      required:
        - roomId
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
    VideoSourceSetPayload:
      type: object
      required:
        - roomId
        - url
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        url:
          type: string
          format: uri
          example: https://www.youtube.com/watch?v=dQw4w9WgXcQ
        duration:
          type: number
          minimum: 0
          nullable: true
          example: 212
    VideoControlPayload:
      type: object
      required:
        - roomId
        - currentTime
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        currentTime:
          type: number
          minimum: 0
          example: 35.4
    RoomPresenceEvent:
      type: object
      required:
        - roomId
        - userId
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
    WhiteboardRoomPayload:
      type: object
      required:
        - roomId
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
    WhiteboardState:
      type: object
      required:
        - id
        - roomId
        - enabled
        - snapshot
        - updatedByUserId
        - createdAt
        - updatedAt
      additionalProperties: false
      properties:
        id:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        roomId:
          $ref: '#/components/schemas/Uuid'
        enabled:
          type: boolean
          example: true
        snapshot:
          $ref: '#/components/schemas/WhiteboardSnapshot'
        updatedByUserId:
          oneOf:
            - $ref: '#/components/schemas/Uuid'
            - type: 'null'
        createdAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
        updatedAt:
          oneOf:
            - type: string
              format: date-time
            - type: 'null'
    WhiteboardSnapshot:
      type: object
      required:
        - strokes
      additionalProperties: false
      properties:
        strokes:
          type: array
          items:
            $ref: '#/components/schemas/WhiteboardStroke'
    WhiteboardPoint:
      type: object
      required:
        - x
        - y
      additionalProperties: false
      properties:
        x:
          type: number
          minimum: 0
          maximum: 1
          example: 0.42
        y:
          type: number
          minimum: 0
          maximum: 1
          example: 0.31
        pressure:
          type: number
          minimum: 0
          maximum: 1
          example: 0.8
    WhiteboardStroke:
      type: object
      required:
        - id
        - userId
        - color
        - width
        - points
        - createdAt
      additionalProperties: false
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
        color:
          type: string
          pattern: '^#[0-9a-fA-F]{6}$'
          example: '#ffcc00'
        width:
          type: number
          minimum: 1
          maximum: 48
          example: 4
        points:
          type: array
          minItems: 1
          maxItems: 10000
          items:
            $ref: '#/components/schemas/WhiteboardPoint'
        createdAt:
          type: string
          format: date-time
    WhiteboardStrokeStartPayload:
      type: object
      required:
        - roomId
        - strokeId
        - color
        - width
        - point
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        strokeId:
          $ref: '#/components/schemas/Uuid'
        color:
          type: string
          pattern: '^#[0-9a-fA-F]{6}$'
          example: '#ffcc00'
        width:
          type: number
          minimum: 1
          maximum: 48
          example: 4
        point:
          $ref: '#/components/schemas/WhiteboardPoint'
    WhiteboardStrokeStartEvent:
      type: object
      required:
        - roomId
        - strokeId
        - userId
        - color
        - width
        - point
        - ts
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        strokeId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
        color:
          type: string
          pattern: '^#[0-9a-fA-F]{6}$'
          example: '#ffcc00'
        width:
          type: number
          minimum: 1
          maximum: 48
          example: 4
        point:
          $ref: '#/components/schemas/WhiteboardPoint'
        ts:
          type: string
          format: date-time
    WhiteboardStrokeAppendPayload:
      type: object
      required:
        - roomId
        - strokeId
        - points
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        strokeId:
          $ref: '#/components/schemas/Uuid'
        points:
          type: array
          minItems: 1
          maxItems: 256
          items:
            $ref: '#/components/schemas/WhiteboardPoint'
    WhiteboardStrokeAppendEvent:
      type: object
      required:
        - roomId
        - strokeId
        - userId
        - points
        - ts
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        strokeId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
        points:
          type: array
          minItems: 1
          maxItems: 256
          items:
            $ref: '#/components/schemas/WhiteboardPoint'
        ts:
          type: string
          format: date-time
    WhiteboardStrokeEndPayload:
      type: object
      required:
        - roomId
        - strokeId
        - color
        - width
        - points
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        strokeId:
          $ref: '#/components/schemas/Uuid'
        color:
          type: string
          pattern: '^#[0-9a-fA-F]{6}$'
          example: '#ffcc00'
        width:
          type: number
          minimum: 1
          maximum: 48
          example: 4
        points:
          type: array
          minItems: 1
          maxItems: 10000
          items:
            $ref: '#/components/schemas/WhiteboardPoint'
    WhiteboardStrokeEndEvent:
      type: object
      required:
        - roomId
        - stroke
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        stroke:
          $ref: '#/components/schemas/WhiteboardStroke'
    VideoSourceType:
      type: string
      enum:
        - youtube
        - direct
    VideoState:
      type: object
      required:
        - id
        - roomId
        - sourceUrl
        - sourceType
        - providerVideoId
        - playing
        - currentTime
        - effectiveCurrentTime
        - duration
        - updatedByUserId
        - createdAt
        - updatedAt
        - serverTime
        - serverTimestamp
      additionalProperties: true
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        roomId:
          $ref: '#/components/schemas/Uuid'
        sourceUrl:
          type: string
          format: uri
        sourceType:
          $ref: '#/components/schemas/VideoSourceType'
        providerVideoId:
          type: string
          nullable: true
          example: dQw4w9WgXcQ
        playing:
          type: boolean
          example: false
        currentTime:
          type: number
          minimum: 0
          example: 35.4
        effectiveCurrentTime:
          type: number
          minimum: 0
          description: Позиция воспроизведения, рассчитанная backend на момент serverTimestamp.
          example: 36.1
        duration:
          type: number
          minimum: 0
          nullable: true
          example: 212
        updatedByUserId:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        serverTime:
          type: string
          format: date-time
        serverTimestamp:
          type: integer
          format: int64
          description: Unix milliseconds backend-времени, на котором рассчитан effectiveCurrentTime.
          example: 1785628801200
    UserSnapshot:
      type: object
      required:
        - id
        - username
        - discriminator
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        username:
          type: string
          example: donu
        discriminator:
          type: string
          example: '1234'
        avatarUrl:
          type: string
          nullable: true
    ChatSendPayload:
      type: object
      required:
        - roomId
        - text
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        text:
          type: string
          minLength: 1
          maxLength: 2000
          example: Привет
    ChatEditPayload:
      type: object
      required:
        - roomId
        - messageId
        - text
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        messageId:
          $ref: '#/components/schemas/Uuid'
        text:
          type: string
          minLength: 1
          maxLength: 2000
          example: Новый текст
    ChatDeletePayload:
      type: object
      required:
        - roomId
        - messageId
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        messageId:
          $ref: '#/components/schemas/Uuid'
    ChatTypingPayload:
      type: object
      required:
        - roomId
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        isTyping:
          type: boolean
          example: true
    ChatReadPayload:
      type: object
      required:
        - roomId
        - messageId
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        messageId:
          $ref: '#/components/schemas/Uuid'
    ChatTypingEvent:
      type: object
      required:
        - roomId
        - userId
        - isTyping
        - ts
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
        isTyping:
          type: boolean
          example: true
        ts:
          type: string
          format: date-time
    ChatReadState:
      type: object
      required:
        - roomId
        - userId
        - lastReadMessageId
        - lastReadAt
        - unreadCount
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
        lastReadMessageId:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          nullable: true
        lastReadAt:
          type: string
          format: date-time
          nullable: true
        unreadCount:
          type: integer
          minimum: 0
          example: 3
    ChatMessage:
      type: object
      required:
        - id
        - roomId
        - userId
        - text
        - editedAt
        - deletedAt
        - deletedByUserId
        - createdAt
        - updatedAt
      properties:
        id:
          $ref: '#/components/schemas/Uuid'
        roomId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
        text:
          type: string
          nullable: true
          example: Привет
        editedAt:
          type: string
          format: date-time
          nullable: true
        deletedAt:
          type: string
          format: date-time
          nullable: true
        deletedByUserId:
          allOf:
            - $ref: '#/components/schemas/Uuid'
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        user:
          $ref: '#/components/schemas/UserSnapshot'
    VoiceRoomPayload:
      type: object
      required:
        - roomId
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
    VoiceUserEvent:
      type: object
      required:
        - roomId
        - userId
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
    VoiceSignalPayload:
      type: object
      required:
        - roomId
        - targetUserId
        - signal
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        targetUserId:
          $ref: '#/components/schemas/Uuid'
        signal:
          type: object
          additionalProperties: true
    VoiceSignalEvent:
      type: object
      required:
        - roomId
        - fromUserId
        - signal
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        fromUserId:
          $ref: '#/components/schemas/Uuid'
        signal:
          type: object
          additionalProperties: true
    VoiceIceCandidatePayload:
      type: object
      required:
        - roomId
        - targetUserId
        - candidate
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        targetUserId:
          $ref: '#/components/schemas/Uuid'
        candidate:
          type: object
          additionalProperties: true
    VoiceIceCandidateEvent:
      type: object
      required:
        - roomId
        - fromUserId
        - candidate
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        fromUserId:
          $ref: '#/components/schemas/Uuid'
        candidate:
          type: object
          additionalProperties: true
    VoiceMutePayload:
      type: object
      required:
        - roomId
        - isMuted
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        isMuted:
          type: boolean
          example: true
    VoiceMuteEvent:
      type: object
      required:
        - roomId
        - userId
        - isMuted
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
        isMuted:
          type: boolean
          example: true
    VoiceMuteParticipantPayload:
      type: object
      required:
        - roomId
        - targetUserId
        - isMuted
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        targetUserId:
          $ref: '#/components/schemas/Uuid'
        isMuted:
          type: boolean
          example: true
    VoiceParticipantMutedEvent:
      type: object
      required:
        - roomId
        - targetUserId
        - mutedByUserId
        - isMuted
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        targetUserId:
          $ref: '#/components/schemas/Uuid'
        mutedByUserId:
          $ref: '#/components/schemas/Uuid'
        isMuted:
          type: boolean
          example: true
    VoiceSpeakingPayload:
      type: object
      required:
        - roomId
        - isSpeaking
      additionalProperties: false
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        isSpeaking:
          type: boolean
          example: true
        audioLevel:
          type: number
          minimum: 0
          maximum: 1
          nullable: true
          example: 0.72
    VoiceSpeakingEvent:
      type: object
      required:
        - roomId
        - userId
        - isSpeaking
        - audioLevel
        - ts
      properties:
        roomId:
          $ref: '#/components/schemas/Uuid'
        userId:
          $ref: '#/components/schemas/Uuid'
        isSpeaking:
          type: boolean
          example: true
        audioLevel:
          type: number
          minimum: 0
          maximum: 1
          nullable: true
          example: 0.72
        ts:
          type: string
          format: date-time
    OkAck:
      type: object
      required:
        - ok
      properties:
        ok:
          type: boolean
          example: true
    OkRoomAck:
      type: object
      required:
        - ok
        - roomId
      properties:
        ok:
          type: boolean
          example: true
        roomId:
          $ref: '#/components/schemas/Uuid'
    VideoStateAck:
      type: object
      required:
        - ok
        - state
      properties:
        ok:
          type: boolean
          example: true
        state:
          oneOf:
            - $ref: '#/components/schemas/VideoState'
            - type: 'null'
    ChatMessageAck:
      type: object
      required:
        - ok
        - message
      properties:
        ok:
          type: boolean
          example: true
        message:
          $ref: '#/components/schemas/ChatMessage'
    ChatReadStateAck:
      type: object
      required:
        - ok
        - readState
      properties:
        ok:
          type: boolean
          example: true
        readState:
          $ref: '#/components/schemas/ChatReadState'
    WhiteboardStateAck:
      type: object
      required:
        - ok
        - state
      properties:
        ok:
          type: boolean
          example: true
        state:
          $ref: '#/components/schemas/WhiteboardState'
    WhiteboardStrokeStartAck:
      type: object
      required:
        - ok
        - event
      properties:
        ok:
          type: boolean
          example: true
        event:
          $ref: '#/components/schemas/WhiteboardStrokeStartEvent'
    WhiteboardStrokeAppendAck:
      type: object
      required:
        - ok
        - event
      properties:
        ok:
          type: boolean
          example: true
        event:
          $ref: '#/components/schemas/WhiteboardStrokeAppendEvent'
    WhiteboardStrokeEndAck:
      type: object
      required:
        - ok
        - stroke
        - state
      properties:
        ok:
          type: boolean
          example: true
        stroke:
          $ref: '#/components/schemas/WhiteboardStroke'
        state:
          $ref: '#/components/schemas/WhiteboardState'
